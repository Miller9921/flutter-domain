name: domain
description: Domain layer - Pure business logic following Clean Architecture
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  equatable: ^2.0.5
  dartz: ^0.10.1

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
